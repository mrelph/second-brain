# second-brain

`second-brain` is a TypeScript CLI for scaffolding and eventually maintaining a persistent, Obsidian-friendly personal knowledge base built around the LLM Wiki pattern.

## Current status

The initial CLI scaffold is in place with an `init` command that creates the knowledge-base directory structure, starter wiki files, and an `AGENTS.md` schema file.
